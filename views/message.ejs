<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Message View</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <div class="container">
      <header>
        <h1>Message View</h1>
      </header>

      <main class="form-container">
        <article>
          <p class="message-text" style="font-size: 1.2rem; margin-bottom: 1rem">
            <%= message.message_text %>
          </p>
          <p class="message-meta">
            <span>Posted by <strong><%= message.username %></strong></span>
            <span>&middot;</span>
            <time datetime="<%= message.added.toISOString() %>">
              <%= message.added.toLocaleDateString("en-US", { month: 'long',
              day: 'numeric', year: 'numeric', hour: 'numeric', minute:
              '2-digit' }) %>
            </time>
          </p>
        </article>
      </main>

      <div class="page-actions">
        <a href="/" class="btn">&larr; Back to All Messages</a>
      </div>
    </div>
  </body>
</html>